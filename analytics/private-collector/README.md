# Analytics Private Collector (Disabled)

This placeholder directory exists to prevent worktree synchronization errors.
The original analytics implementation was removed at the user's request.

- `worker.js`: returns HTTP 410 to signal that the endpoint is no longer active.
- No analytics data is collected or processed.

If you need to reintroduce analytics, replace these files with the proper implementation.
